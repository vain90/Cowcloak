services:
  cowcloak:
    build:
      context: .
    image: cowcloak:local
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${COWCLOAK_PORT:-8080}:8000"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
